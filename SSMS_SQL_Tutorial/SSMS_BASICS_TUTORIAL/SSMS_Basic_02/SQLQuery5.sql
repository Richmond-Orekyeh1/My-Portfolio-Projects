-- To check the highest salary on the list
SELECT MAX(Salary)
FROM EmployeeSalary


-- To check the lowest salary on the list
SELECT MIN(Salary)
FROM EmployeeSalary


-- To check the average salary on the list
SELECT AVG(Salary)
FROM EmployeeSalary

	-- To check all the data in the salary table
SELECT *
FROM SQLTutorial.dbo.EmployeeSalary